/*jslint devel: true, browser: true */
/*global jQuery: false, app: false */

app.charts.TimeSeries = (function ($) {
    'use strict';

    function ctor(options) {
        alert('hi');
    }

    // set it
    return ctor;
}(jQuery));